[Definition]
failregex = ^<HOST> .* "(GET|POST|HEAD).*HTTP.*" ([45]\d\d) .*
ignoreregex = .*(robots\.txt|sitemap\.xml|favicon\.ico|\.(?:css|js|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot))(\?.*)?$
              (?:GoogleBot|bingbot|Baidu|YandexBot|DuckDuckBot|Sogou)/